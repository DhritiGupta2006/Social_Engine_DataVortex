# DATA VORTEX — Cleaning Decision Log

Generated by `src/02_clean_data.py`. Every transformation below is deterministic and reproducible from the raw files in `data/raw/`. No values were invented; missing data is preserved as missing.

## Users cleaning

- Raw rows: 1500
- Whitespace trimmed on `location`/`language`; `account_created` normalized to ISO `YYYY-MM-DD`; `follower_count` cast to int.
- Exact duplicate rows removed: 0
- Final rows: 1500 (no other changes needed — see audit report)

## Posts cleaning

- Raw rows: 12360
- Normalized literal `NULL`/empty-string placeholders to real missing values: platform=1846, text_content=1770, likes=1858. **These rows are kept** (not dropped) — the missing field just isn't usable for analyses that need it.
- Decoded HTML entities (e.g. `&amp;` -> `&`) and trimmed stray whitespace in `text_content` and `platform`.
- Parsed 3 mixed timestamp formats (unix epoch, ISO 8601, `DD-MM-YYYY`) into a single standardized `event_time` column. Unparseable: 0.
- Corrected 525 negative `likes` values by taking the absolute value. Justification: `abs(negative likes)` and positive `likes` have near-identical mean/std/range (see audit report) — this is a sign-flip corruption, not a separate population, so recovering the magnitude is a correction, not a fabrication. `likes` is still missing (NaN) where the raw value was itself missing.
- Removed 360 exact duplicate rows (all fields identical, confirmed in the audit to be re-ingestion artifacts affecting 352 distinct post_ids). Rows were matched on every column INCLUDING the raw `timestamp`, so only true re-ingested copies were removed.
- Rows referencing a `user_id` not present in the users table: 0 (none found; no rows removed for this reason).
- Final rows: 12000 (from 12360 raw rows)
- Final missingness retained as NaN: platform=1784, text_content=1711, likes=1814

